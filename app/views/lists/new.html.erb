<h1>New List</h1>

<%= form_with model: @list do |f| %>
  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <%= f.submit "Create List" %>
<% end %>

<%= link_to "Back to lists", lists_path %>
